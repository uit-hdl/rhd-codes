Takes as input a coordinate file, -m <image/source/location> -t <digits or text> -w <number of cores to be used> --db <a SQLite database file to be created to contain the images> -c <column to be extracted>

Example - Coordinates.txt -m /media/ft1950/ -t digits -w 6 --db example.db -c 13
